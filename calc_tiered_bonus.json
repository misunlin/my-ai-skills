{
    "name": "calc_tiered_bonus",
    "language": "python",
    "description": "計算 5%、8%、12% 三階層累進獎金工具。直接傳入 total_sales 進行結算。",
    "parameters": {
        "type": "object",
        "properties": {
            "total_sales": { "type": "number", "description": "業績數字" }
        },
        "required": ["total_sales"]
    },
    "handlerCode": "import json\nsales = float(args.get('total_sales', 0))\nbonus = 0\nif sales <= 100000:\n    bonus = sales * 0.05\nelif sales <= 300000:\n    bonus = 5000 + (sales - 100000) * 0.08\nelse:\n    bonus = 5000 + 16000 + (sales - 300000) * 0.12\nreturn {'status': 'success', 'calculated_bonus': bonus, 'message': 'Python 計算完成：' + str(bonus)}"
}
